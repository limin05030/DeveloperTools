# DeveloperTools

`Developer Tools` 中集合了开发过程中常用的功能，比如 `哈希计算`、`编码转换`、`时间转换`等。

# macOS 安装说明

下载 `DeveloperTools-macos.tar.gz`，解压后得到 `DeveloperTools.app
`，然后将 `DeveloperTools.app` 复制到 `/Applications` 目录中。

第一次启动时，由于 app 的签名问题，会导致被系统阻止，需要手动允许：
![](screenshot/1.png)